# -*- coding: utf-8 -*-
"""
Spyder Editor

This is a temporary script file.
"""


import matplotlib.pyplot as plt
import scipy.integrate as it
import numpy as np
def ricatti(P):
    # sympy has a matrix class with matrices in
    return A.transpose()*P+P*A - (P*B+N)*R.inverse()*(B.transpose()*P*N.transpose() + Q;

def finiteHorizonLQR(A,B,F,Q,R): # ignoring the N matrix, not usually needed
    
    


time = np.arange(0, 15/j, 0.01) 
Ps = it.odeint(order4,[0.0],time).T 
axes[2].plot(time, -vel, color='k')
axes[2].set_title('order 4')
